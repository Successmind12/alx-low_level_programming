#include "main.h"

/**
 * function that returns the length of a string.
 * @n: input parameters include char *s
 * Return len;
 */

int _strlen(char *s)
{
int length = 0;

while (s[length] != '\0')
length++;

				         }

return (length);

}


