#include "main.h"



int main()

{	

		/* Get the character to be writtten */

		char ch = 'a';

	       /* Write the character to stdout */

	        for (ch = 'a'; ch <= 'z'; ch++ )
                putchar(ch);
                putchar('\n');
                return (0);

} 
